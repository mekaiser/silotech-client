import React from 'react';
import Header from '../Header/Header';

const NotFound = () => {
    return (
        <div className="App">
            <Header></Header>
            <h1 className="mt-5">Oops, site not found!</h1>
        </div>
    );
};

export default NotFound;